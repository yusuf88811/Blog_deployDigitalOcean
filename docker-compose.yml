version: '3'


services:
  app:
    build: .
    depends_on:
      - db
    ports:
      - 8000:8000
    env_file:
      - .env

  db:
    image: postgres:9.6
    environment:
      - NAME=new_blog-02
      - USER=postgres
      - PASSWORD=yusuf123

    volumes:
      - .:/code


